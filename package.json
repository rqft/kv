{
  "name": "@rqft/kv",
  "version": "1.0.04",
  "description": "",
  "main": "dist/index.js",
  "nodemonConfig": {
    "ignore": [
      "kv/*.json"
    ]
  },
  "scripts": {
    "pub": "tsc && git commit -a -m ok && git push && npm version patch && npm publish --access public"
  },
  "type": "commonjs",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.6.5"
  }
}
